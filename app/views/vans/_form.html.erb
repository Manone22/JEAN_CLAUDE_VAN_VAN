<div class="container" style="items-align">
  <h1>Créer un nouveau van</h1>
  <%= simple_form_for(@van) do |f| %>
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

    <div class="form-inputs">
      <%= f.input :name, class: "form-fields" %>
      <%= f.input :description, class: "form-fields" %>
      <%= f.input :seat, class: "form-fields" %>
      <%= f.input :location, class: "form-fields" %>
      <%= f.input :date, class: "form-fields" %>
      <%= f.input :price, class: "form-fields" %>
      <%= f.input :image_url, class: "form-fields" %>
    </div>

    <div class="form-button">
      <%= f.button :submit, class: "form-button-text" %>
    </div>
  <% end %>
</div>

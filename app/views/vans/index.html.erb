<p id="notice"><%= notice %></p>

<h1>JC Van Van</h1>

<p><%= link_to 'Rent my van', new_van_path %></p>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>Description</th>
      <th>Place</th>
      <th>Adresse</th>
      <th>Disponibilité</th>
      <th>Prix</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vans.each do |van| %>
      <tr>
        <td><%= van.name %></td>
        <td><%= van.description %></td>
        <td><%= van.seat %></td>
        <td><%= van.location %></td>
        <td><%= van.date %></td>
        <td><%= van.price %></td>
        <td><%= van.image_url %></td>
        <td><%= link_to 'See details', van_path(van) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back to home page', root_path %>
